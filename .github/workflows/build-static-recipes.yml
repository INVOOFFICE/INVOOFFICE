# ============================================================
# INVOO OFFICE — Build static blog pages
# Déclenché quand blogs.json change (via Google Apps Script)
# Génère blogs/<slug>/index.html + sitemap.xml + llms.txt + robots.txt
# ============================================================
name: Build static blog pages

on:
  push:
    branches: [main]
    paths:
      - "blogs.json"
      - "scripts/build-blog-pages.mjs"
      - ".github/workflows/build-static-recipes.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: static-blogs-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Generate blogs/ pages, sitemap.xml, llms.txt, robots.txt
        run: node scripts/build-blog-pages.mjs

      - name: Commit and push if changed
        env:
          BRANCH: ${{ github.ref_name }}
        run: |
          set -e
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add blogs/ sitemap.xml llms.txt robots.txt
          if git diff --staged --quiet; then
            echo "Aucun changement détecté — pages déjà à jour."
            exit 0
          fi
          git commit -m "chore: régénération pages blog statiques [skip ci]"
          git pull --rebase origin "$BRANCH"
          git push origin "HEAD:refs/heads/$BRANCH"
